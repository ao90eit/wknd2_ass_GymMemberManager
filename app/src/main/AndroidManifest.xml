<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aoinc.wknd2_ass_gymmembermanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wknd2_ass_GymMemberManager">
        <activity android:name=".views.AddMemberActivity"></activity>
        <activity android:name=".views.AccountSettingsActivity" />
        <activity android:name=".views.MemberDetailActivity" />
        <activity android:name=".views.MemberListActivity" />
        <activity android:name=".views.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>